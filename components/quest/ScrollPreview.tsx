// This file is intentionally left empty.
//
// `ScrollPreview` was an earlier draft of the /create live preview that
// mirrored the 3D scroll-on-the-board look. It was replaced in favor of
// reusing `<QuestCard variant="scene" />` (the same parchment card the
// recipient sees on /invite), so the sender previews exactly what they
// ship. This file can be deleted at your convenience.
export {};
